if SHIELD_KAASCH_KEYS_LEFT

# Name must be less than 16 characters long!
config ZMK_KAASCH_KEYS
    default "Kaasch Keys"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_KAASCH_KEYS_LEFT || SHIELD_KAASCH_KEYS_RIGHT

config ZMK_SPLIT
    default y

endif
